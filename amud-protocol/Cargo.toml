[package]
name = "amud-protocol"
version = "1.9.3"
edition = "2021"
description = "Shared wire types for AMUD dashboard ↔ agent IPC"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
